# PUBLIC_INTERFACE
# Base URL for backend API. Do not include trailing slash.
# Example: http://localhost:8080
REACT_APP_API_BASE_URL=
# Optionally, set the site URL used for auth redirects if applicable in future:
# REACT_APP_SITE_URL=
